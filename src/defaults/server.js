 const { Request, Response } = require("express");
 const {client} = require("../../bot/index");

 module.exports = {
     name: "/",
     type: "",
     /**
      * @param {Request} req 
      * @param {Response} res 
      */
     run: async (req, res) => {
         
     }
 }